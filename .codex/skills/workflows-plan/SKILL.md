---
name: workflows-plan
description: Use when the user says "$workflows-plan", "/workflows:plan", asks for an iworkflow implementation plan, or wants requirements turned into tasks
---

# Workflows Plan

Read `.claude/commands/workflows/plan.md` completely and execute that workflow.
Translate `/workflows:plan` to `$workflows-plan` for Codex usage.

Preserve iworkflow hard rules: subscription-only workers, deterministic
orchestration, stdlib-only core, and subscription-free verification first.
