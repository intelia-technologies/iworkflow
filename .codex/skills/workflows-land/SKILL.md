---
name: workflows-land
description: Use when the user says "$workflows-land", "/workflows:land", or asks Codex to validate and prepare an iworkflow change for PR
---

# Workflows Land

Read `.claude/commands/workflows/land.md` completely and execute that workflow.
Translate `/workflows:land` to `$workflows-land` for Codex usage.

Preserve iworkflow hard rules: subscription-only workers, deterministic
orchestration, stdlib-only core, and subscription-free verification first.
